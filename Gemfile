source "https://rubygems.org"

# should be in gemspec but bundler isn't auto-including in
# Bundler.require :default, Rails.env
gem "jquery-rails", "~> 2.3.0"

group :test do
  gem 'cucumber-rails', require: false
end

gemspec
